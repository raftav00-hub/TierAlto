{
  "name": "tieralto",
  "version": "1.0.0",
  "description": "TierAlto website",
  "private": true,
  "scripts": {
    "dev": "npx serve . --port 3000",
    "lint:css": "npx stylelint \"src/css/**/*.css\"",
    "lint:js": "npx eslint \"src/js/**/*.js\"",
    "lint": "npm run lint:css && npm run lint:js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0",
    "serve": "^14.0.0"
  }
}
